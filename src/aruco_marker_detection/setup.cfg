[develop]
script_dir=$base/lib/aruco_marker_detection
[install]
install_scripts=$base/lib/aruco_marker_detection

[pep257]
match_dir=(?!build|install|log|\.git)[^\.].*

[tool:pytest]
norecursedirs=build install log .git
